package org.ITSense.Almacen.app.Almacenapp.security.Enums;

public enum RolNombre {
    //El aplicativo tiene dos roles:
    // admin(Tiene todos los permisos CRUD) y user(ve las torres)
    ROLE_ADMIN, ROLE_USER
}
